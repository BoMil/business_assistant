/// Shared layout constants used across all screens.
class ThemeConstants {
  /// Horizontal padding applied to every full-width screen.
  static const double pagePadding = 18;
  static const double headerPadding = 10;
}
